---
config_directory: "{{ config_home }}/sway"
dependencies:
  - sway
  - swayidle
  - swaylock
  - wl-clipboard
  - pipewire
  - brightnessctl

  # Install terminal as dependency of Sway
  - "{{ terminal }}"
