---
- name: Setup and configure dotfiles
  hosts: all
  roles:
    - fish
    - bash
    - firefox
    - thunderbird
    - neovim
    - rofi
    - sway
    - swaylock
    - i3status
    - mako
    - tmux
    - fontconfig
    - zathura
    - "terminals/{{ terminal }}"
  vars_files:
    - theme.yml
    - variables.yml
