---
- name: Setup and configure dotfiles
  hosts: all
  roles:
    - fish
    - neovim
    - rofi
    - sway
    - tmux
    - dotfiles
    - fontconfig
    - zathura
    - "terminals/{{ terminal }}"
  vars_files:
    - theme.yml
    - variables.yml
